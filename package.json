{
  "name": "rumblr",
  "version": "0.1.0",
  "description": "Minimal scrolling mirror of a Tumblr blog",
  "type": "module",
  "private": true,
  "scripts": {
    "scrape": "node scripts/scrape.mjs",
    "build": "eleventy",
    "serve": "eleventy --serve",
    "dev": "npm run scrape && npm run serve",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sharp": "^0.35.0"
  }
}
